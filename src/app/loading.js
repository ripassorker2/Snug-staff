const Loader = () => {
    return (
        <div className="flex justify-center items-center h-screen">
            <div className="rounded-full h-16 w-16 bg-purple-400 animate-ping"></div>
        </div>
    );
};

export default Loader;
